version: '3.8'

services:
  schichtplaner:
    container_name: schichtplaner
    image: ghcr.io/donpat1to/schichtenplaner:v1.0.0
    ports:
      - "3002:3002"
    volumes:
      - app_data:/app/data
    restart: unless-stopped

volumes:
  app_data: